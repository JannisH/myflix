%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followed.each do |followed|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(followed.email.downcase)}?s=40")
            = link_to followed.full_name, users_path(followed)
          %td.extra-padding #{(followed.video_queue != nil) ? followed.video_queue.videos.size : 0}
          %td.extra-padding #{followed.followers.size}
          %td.extra-padding
            = link_to user_unfollow_path(followed), method: :delete do
              %i.glyphicon.glyphicon-remove
